# px4_drv 開発指針

この文書は、px4_drv を変更する AI エージェントと開発者が、既存の設計を保ちながら安全に作業するためのリポジトリ共通指針です。  
利用方法と対応機種は [README.md](README.md) を参照し、WinSCard_PX4 固有の設計は [winusb/src/WinSCard_PX4/README.md](winusb/src/WinSCard_PX4/README.md) を参照してください。

## ソフトウェアの性質

px4_drv は、デジタル放送の TS を長時間連続して受信するためのデバイスドライバとユーザー空間コンポーネントです。  
利用者は録画処理を数時間から数日間継続する可能性があり、一度放送されたら再放送されない（録画失敗が永続的な喪失になる）ケースも多数あります。  
短時間の停止、数パケットの欠落、終了時だけ発生する競合も、録画結果の破損として利用者へ影響します。  
機能が動くことだけを完成条件にせず、ドロップ、同期エラー、連続性エラー、リソースの寿命、長時間後の状態を確認してください。

既存実装は多数の機種と環境で使われ、長期間の修正を重ねています。  
一見すると遠回りな待機、ロック、バッファ、電源手順には、過去のハードウェア差や競合を避ける理由が含まれる可能性があります。  
意図を確認できないコードを、一般的な設計原則だけで短縮しないでください。

一方、利用実績があることは、すべてのエッジケースが解消済みである証拠にはなりません。  
実際に、チャンネル切り替え時の TS 同期判定、USB 切断後のカード状態、T=1 の再送など、通常利用では見えにくい不具合が後から判明しています。  
既存動作の維持と未解決条件の調査を両立し、確認できた条件だけを修正対象にしてください。

## リポジトリの構成

`driver/` は Linux カーネルドライバです。  
USB ブリッジ、機種別デバイス、受信機、チューナー、復調器をカーネル内で管理し、TS をデバイスファイルとして公開します。

`winusb/` は Windows の WinUSB 版です。  
`BonDriver_PX4.dll` は DTV アプリケーションとの境界を担当し、`DriverHost_PX4.exe` が USB デバイス、受信機、ストリーム、内蔵カードリーダーをユーザー空間で共有管理します。  
`WinSCard_PX4` はアプリケーションローカルの `WinSCard.dll` として、内蔵カードリーダーを Windows WinSCard API へ統合します。

`fwtool/` はデバイスで使うファームウェアを取得、変換する補助ツールです。  
`dkms/`、`px4_drv-dkms-mkdeb/`、`build_deb.sh` は Linux 版の配布と DKMS パッケージを扱います。

## コードから読み取れる設計

初期実装と大部分の安定化を担った nns779 氏本人の説明がコード中にない箇所については、意図を断定できません。  
ただし、コミット履歴と現行コードから、次の設計上の傾向を確認できます。

### 機種差の局所化

PX4、PXMLT、ISDB2056、ISDBT2071 などの差は、機種別デバイス実装へ閉じ込められています。  
IT930x の共通操作、受信機の共通契約、チューナーと復調器の制御を分け、呼び出し側が製品名ごとの条件分岐を持たない構造です。  
新機種への対応では、既存機種の条件を広げて流用する前に、電源、GPIO、入力数、TS のタグ形式、カードリーダーの有無が同じか確認してください。

### 所有権の明示

Linux 版はカーネルの参照管理とデバイス寿命を使い、WinUSB 版は `shared_ptr`、マネージャー、非コピー可能なサーバークラスを使っています。  
USB の抜去後も受信スレッドや名前付きパイプ接続が残るため、登録表から削除したことだけでオブジェクトを解放できません。  
利用中の処理が保持する参照と、デバイスが利用可能かを示す状態を分けて管理してください。

### 状態を持つホストプロセス

WinUSB 版は、BonDriver ごとに USB デバイスを直接開く構成を避け、DriverHost_PX4 にデバイス状態を集約しています。  
この構成により、複数チューナー、共有電源、ホットプラグ、複数アプリケーションの競合を1つの所有者が調停します。  
DriverHost_PX4 の多重起動防止、アイドル終了、名前付きパイプ接続の寿命は、単なる起動補助ではなくデバイス所有権の一部です。

### 受信経路の分離

制御要求と TS ストリームは別のサーバーと接続で扱われます。  
カードリーダー機能も独立したサーバーとして追加され、起動失敗が既存の受信処理を中止しない構造になっています。  
補助機能を追加するときは、TS 受信スレッド、受信バッファ、デバイスの制御ロックへ長時間の待機を持ち込まないでください。

### 明示的な失敗

下位層は Linux の負の errno または WinSCard 用の状態へ変換可能なエラーを返し、呼び出し側が失敗を判断します。  
切断や形式不正を成功扱いにするフォールバックは、壊れた状態を長時間残すため避けてください。  
再試行は、再実行して安全な操作と、実機で回復が確認されたエラーだけへ限定します。

## 変更の基本原則

### 受信結果の維持

カードリーダー、ログ、状態監視、終了処理などを変更する場合も、TS の受信結果を検証対象に含めます。  
受信と無関係に見える制御転送が同じ USB ブリッジを占有し、TS の読み出し時刻へ影響することがあります。  
コンパイル成功、カード単体の APDU 成功、保存済み TS の復号成功だけで受信への影響を判定しないでください。

### 最小限の差分

問題を再現したら、原因となる状態とコードの範囲を特定し、その範囲を直接修正します。  
同じ変更に無関係な命名変更、整形、抽象化の置換を混ぜないでください。  
差分を小さくする目的はレビュー量の削減だけではなく、実機で確認すべき条件を限定し、問題が起きたときに変更との対応を追えるようにすることです。  
構造的な修正に広い書き換えが必要な場合は、先に理由、維持する契約、移行方法、試験範囲を文書化してください。

### 実測に基づく値

待機時間、USB 転送長、バッファ数、再送回数を一般論から決めないでください。  
対象機種で通常時と負荷時の値を測り、正常処理を切らず、異常処理を有限時間で終える境界を選びます。  
タイムアウトは各ループで作り直さず、操作全体の絶対期限として扱うことを基本とします。

### 状態破棄の完全性

デバイスの切断、カードの抜去、通信形式の破損を検出した場合は、再利用できない状態を一括して破棄します。  
フラグだけを無効にして、ATR、連番、電源参照、バッファ位置などを残すと、再接続後に別の機器へ古い状態を適用します。  
どの関数が状態を作り、どの失敗で無効になり、次回どこから再構築するかを確認してください。

### ロック範囲

ロックは、対応する状態とハードウェア操作の一貫性を守る範囲に限定します。  
スリープ、イベント待機、名前付きパイプ I/O、長い USB 応答待ちの間に、TS 配信やデバイス一覧全体のロックを保持しないでください。  
複数のロックを取得する処理では、既存の取得順を確認し、終了処理とホットプラグを含む逆順の入口がないか調べます。

### エラーの境界

USB、機種別デバイス、プロトコル、プロセス間通信、公開 API の各層で、エラーの意味を保って変換します。  
カード未挿入、リーダー切断、共有違反、プロトコル不一致、応答タイムアウトを同じエラーへまとめないでください。  
外付けカードリーダーの処理を System32 へ転送する場合は、Windows が返したエラーを維持します。

## 機種に関する前提

製品名が似ていても、USB ブリッジ、入力数、電源、カード検出 GPIO、TS 形式が同じとは限りません。  
機種判定は既存の `DeviceDefinition` と機種別列挙を使い、USB VID と PID だけの推測で処理を共有しないでください。

### PX4 系

PX-W3U4、PX-Q3U4、PX-W3PE4、PX-Q3PE4 などは、受信機番号を同期バイト上位4bitへ含めた188バイト TS を集約して受信します。  
同期判定では下位4bit の `0x07` と上位の受信機番号を扱い、transport error indicator が立った値を同期として受理しません。  
一部モデルは内蔵カードリーダーを持たないため、`HasCardReader()` を唯一の判定として使います。

### PXMLT 系

PXMLT 系も受信機番号付き TS を使いますが、入力数と電源構成が PX4 系と異なります。  
PX-MLT8PE は1つの筐体に5チューナー側と3チューナー側の USB デバイスを持ちます。  
`PXMLT8PE3` は筐体内の3チューナー側を示すコード上の区別であり、単独製品として機能表や文書へ追加しないでください。  
カードリーダーを持つ側と持たない側を混同せず、共有電源の参照数をチューナー利用とカード利用の双方から更新します。

### ISDB2056 系

ISDB2056、ISDB2056N、PX-M1UR などは通常の `0x47` 同期バイトを持つ TS を受信します。  
同じ IT930x 上で TS ストリームと B-CAS UART の制御転送が進むため、カード通信を追加した試験はリアルタイム受信と同時に実行してください。  
過去にはチャンネル切り替え直後の同期判定とカード通信中の受信負荷で、単体試験から見えない問題が確認されています。

### ISDBT2071 系

ISDBT2071 対応は比較的新しく、機種固有の初期化とカード操作を ISDB2056 と同一だと仮定しないでください。  
既存の暫定対応という履歴も考慮し、変更時は該当ロットの実機確認範囲を明記します。

## TS 処理の不変条件

TS パケットは188バイトを単位として扱います。  
同期を失った場合は、次の受信機番号または `0x47` を探して境界を回復し、前回の端数と今回の入力を正しい順序で連結します。

受信機番号付き TS と通常 TS の同期条件は [`driver/ts_sync.h`](driver/ts_sync.h) の共通関数を使います。  
Linux 版と WinUSB 版へ同じ条件を複製しないでください。

adaptation field を解析する試験や補助ツールでは、長さがパケット内へ収まることを確認してから continuity counter を読みます。  
チャンネル変更時は、変更前の TS、バッファの端数、continuity counter を次のチャンネルへ引き継がないことを確認してください。  
ドロップの判定では、信号品質だけでなく、同期エラー、transport error indicator、continuity counter、空受信時間を分けて記録します。

## 電源と終了処理

USB 機器によっては、複数のチューナーとカードリーダーが同じ電源状態を共有します。  
最初の利用者が電源を入れ、最後の利用者が閉じたときだけ電源を切る参照数の契約を維持してください。  
カードを開いたままチューナーを閉じても電源を切らず、カードを閉じたまま受信中でも電源を切りません。

USB 切断時は `SetAvailability(false)` をロック下で反映し、カードセッション、共有電源のカード参照、受信機の利用可能状態を無効にします。  
終了処理は新しい要求の受付を止め、カード、ストリーム、制御、デバイスの順序関係を確認してから各スレッドを回収します。  
自分が起動していない DriverHost_PX4 や録画プロセスを、試験の都合で停止しないでください。

## WinSCard_PX4 の境界

WinSCard_PX4 は Windows WinSCard API の互換 DLL であり、復号器ではありません。  
B-CAS の ECM 処理、契約情報、放送方式の判断は TvCas や libaribb25 などの上位層が担当します。

WinSCard API が公開する全関数をエクスポートし、内蔵カードリーダーとの統合が不要な関数も System32 へ転送してください。  
内蔵カードリーダーを優先して列挙し、その後へ System32 が管理する外付けカードリーダーを追加する順序を維持します。

製品コードへ B-CAS の ATR 一覧を仮想カード種別として追加しないでください。  
ハードウェアが B-CAS 前提であることと、WinSCard API 層がカード種別を決めることは別の責務です。

## Linux 内蔵カードリーダー対応

Linux 版へ内蔵カードリーダーを追加するときは、https://github.com/soralis0912/it930x_driver_native と [yyya-nico 氏のフォーク](https://github.com/yyya-nico/px4_drv/tree/develop) を通信手順の資料として参照できます。  
これらのコードをそのまま移植する前に、現行カーネルのデバイス寿命、ユーザー空間 API、排他制御、待機、エラー通知へ適合するか確認してください。

特に ATR 完了判定、T=1 の状態機械、絶対期限、抜去後の状態破棄、TS と UART の同時負荷は、WinUSB 版で得た知見を使って再設計します。  
プロトコル処理はハードウェア I/O から分離し、模擬 I/O による自動試験を可能にしてください。

カーネル内へ T=1 の全処理を置くか、ユーザー空間の PC/SC 層へ置くかは、公開 API と既存ソフトウェアの接続方法を調査してから決めます。  
実証コードが動作したことだけで、長時間録画環境へ採用できる品質を確認したことにはなりません。

## 調査の進め方

不具合を修正する前に、受信機、カードリーダー、アプリケーション、対象チャンネル、経過時間、D、E、S の変化を記録します。  
別機種で正常なことは、対象機種のハードウェア故障とソフトウェア不具合のどちらも確定しません。

同じ TS を別リーダーで復号する試験、同じリーダーを保存済み TS で使う試験、対象機器で受信と APDU を同時実行する試験を分けると、失敗する層を限定できます。  
接触不良を疑う場合はカード検出、ATR、APDU エラーの時刻を記録し、TS のドロップとスクランブル増加を混同しないでください。

Web 上の報告や別フォークの修正を利用する場合は、対象コミットと現行コードの差を確認し、同じ条件が存在するかをソースと実機で再検証します。  
公式ドライバの逆解析は、公開ソースと実機だけではコマンドの意味を確定できない場合に限定し、観察した事実と推定を分けて記録してください。

## テスト

### Linux 版

対象カーネルのヘッダーを用意し、`driver/` でカーネルモジュールをビルドします。

```bash
make -C driver
```

ビルドだけで、USB 切断、長時間受信、複数チューナー、対象機種の初期化を確認したことにはなりません。  
ハードウェア変更では対象機種の実機試験を追加し、カーネルログと TS の計数を保存してください。

### WinUSB 版

Visual Studio 2022 と v143 toolset を使い、x86 と x64 の双方をビルドします。

```powershell
Set-Location C:\Develop\px4_drv\winusb
.\build.ps1
```

`build.ps1` はソリューション、TS 同期判定テスト、カード状態遷移テスト、配布物を処理します。  
カードリーダー変更では `winscard_api_test`、`card_reader_reliability`、`card_stream_stress`、`live_b25_reliability` を変更内容に応じて実行します。

チャンネル切り替えや TS バッファ変更では `channel_switch_stress` を実行し、各周期の同期エラー、transport error、continuity error、空受信を確認します。  
実機試験ツールの成功条件を緩めたり、失敗を警告だけへ変更したりしてビルドを通さないでください。

### 検証の報告

実行したコマンド、対象アーキテクチャ、接続した機種、カードの配置、アンテナ接続、試験時間を記載します。

未接続の BS と CS、カードを持たない側のデバイスなど、試験できなかった条件を明記します。  
「映った」だけで完了とせず、D、E、S、APDU 失敗数、最大処理時間を取得できる試験では数値を報告してください。

自動試験の成功を実機試験の代用にせず、実機試験の成功を境界値と状態遷移の自動試験の代用にしません。

## コードと文書

既存の詳細なコメントは、過去の機種差や待機理由を残している可能性があるため、内容が誤っていると確認できるまで削除しないでください。  
新しい状態、分岐、待機、再試行、機種差には、コードだけから分からない理由を日本語コメントで残します。  
コメントへ作業中の経緯や一時的な試験値を書かず、将来も必要な制約と理由を書いてください。

公開 API、名前付きパイプ構造体、設定ファイル、デバイスファイルの契約を変更した場合は、同じ変更で対応する文書と互換性の説明を更新します。  
版番号は [`update_version.sh`](update_version.sh) と共有版情報の生成先を確認し、各リソースヘッダーを手作業だけで変更しないでください。  
WinSCard_PX4 を含む Windows バイナリのプロパティには、製品名と版番号が識別できるリソースを維持します。

## Git と作業領域

作業開始時に `git status --short` を確認し、ステージ済み、未ステージ、未追跡の差分を区別します。  
既存の差分はユーザーまたは別の作業者の所有物として扱い、依頼と無関係な変更を復元、整形、削除しないでください。  
明示的な依頼がない限り、`git add`、ステージ解除、コミット、履歴の書き換え、push を実行しません。

ビルド生成物や調査用ファイルを既存の管理場所へ混ぜず、再生成可能性と所有者を確認せずに削除しないでください。  
コミットを依頼された場合は、Linux ドライバ、DriverHost_PX4、WinSCard_PX4、試験、配布処理など、独立して意味を持つ単位へ分けます。

## 完了条件

変更したコードがビルドできることに加え、該当する自動試験と実機試験を実行して出力を確認します。  
新機能の成功条件だけでなく、既存機種、外付けカードリーダー、チューナー未使用時、USB 切断、終了処理など、変更が触れた既存条件を確認します。

すべての機種や長時間条件を試験できない場合は、確認済みの範囲と未確認の範囲を分けて報告します。  
現象を再現できていない推測は、確定した不具合や修正済みの問題として記録しないでください。
